# Synthetic Case Reference Packs

The hack is live.

## Locked use

1. `02_payment_outage` — **primary polished live demo**.
2. `06_objective_loss_leader` — cross-domain INTENT conformance proof; CLI/test only.
3. `01_order_exception` — backup/generalization reference.
4. `03_market_access` — dramatic backup.
5. `04_semiconductor_rca` — RCA-origin reference.
6. `05_supply_chain_port_delay` — supply-chain generalization reference.

Do not build a second polished UI.

The product generalization claim is:

> the same deterministic permission kernel can consume materially different `case.yaml` inputs without changes to `contract.py`.
