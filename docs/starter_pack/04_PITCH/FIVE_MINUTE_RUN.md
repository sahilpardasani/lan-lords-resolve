# Resolve — Five-Minute Run of Show

## 0:00–0:25 — Problem

“Payment success fell from 98.6% to 79%. An AI can quickly recommend a global failover. But a recommendation is not permission.”

## 0:25–0:45 — Product / local

“Resolve is a local AI troubleshooting and permission loop on the GB10. The model investigates and proposes. Deterministic Python decides whether that exact action has earned permission.”

## 0:45–1:05 — Tempting answer

Show:

`GLOBAL -> Processor B -> 100%`

This looks operationally attractive.

## 1:05–1:45 — Challenge + deterministic block

- scout cites local incident/authorization evidence;
- investigator connects the deployment/routing facts;
- planner proposes global failover;
- challenger exposes the regional/authority conflict;
- deterministic Resolve Contract shows `CONSTRAINTS=FAIL`;
- disposition: **BLOCKED**.

Say:

> “The LLM found the objection. Python owns the consequence.”

## 1:45–2:30 — Targeted evidence + bounded plan

Resolve asks for the exact missing operational evidence needed for a safer alternative.

Reveal traffic/replay evidence.

New candidate:

`Processor B | region=US | traffic=40%`

Rehearsal passes.

Contract becomes:

`WAITING_HUMAN`.

## 2:30–3:15 — Exact approval + mutation

Ask a judge/operator to approve the exact bounded action if practical.

Then change:

`US -> GLOBAL`

Show:

`APPROVAL INVALID`.

Restore the signed candidate.

This is the memorable beat.

## 3:15–3:45 — Commit + independent verify

Execute only the approved bounded simulator change.

Verifier reads the actual simulator state.

Show `VERIFIED`.

## 3:45–4:15 — MongoDB audit + replay

Show the MongoDB-backed event trail:

`evidence -> candidate -> BLOCKED -> new evidence -> bounded candidate -> approval -> invalidation test -> commit -> verification`

Say:

> “MongoDB is our audit substrate. It records the decision trail; it never decides permission.”

## 4:15–4:35 — Local-first / sponsor proof

Show only what is actually proven:

- Qwen inference local on GB10;
- OpenShell policy;
- public HTTP BLOCKED;
- local inference PASS;
- local Resolve tool PASS;
- MongoDB local.

If OpenClaw/NemoClaw were added safely, mention them. Do not apologize if P0 uses only OpenShell; organizer requires at least one.

## 4:35–5:00 — Close

“The AI found a plausible recovery. Resolve proved the unsafe version had not earned permission, bound human authority to the safe version, rejected a material mutation, executed only the signed action, and independently verified the result — locally.”

Final line:

> **The AI recommends. Resolve decides whether that exact recommendation has earned permission.**
