# Empty Competition Repo Scaffold

This directory is intentionally **non-functional**. Copy its directory shape only after kickoff if organizers confirm starter scaffolding is acceptable.

It contains no agent prompts, orchestrator, runtime, case adapter, tool implementation, simulator, API implementation or product UI.

Suggested directories:
- `app/`
- `tests/`
- `ui/`
- `cases/`
- `evidence/`
- `scripts/`

At 10:00 create a new Git repository and make a kickoff baseline commit before writing implementation.
