# Application / test dependencies for the Resolve Python surface.
# Do not pin GB10 host/container packages here. vLLM and the model
# stay in the frozen NVIDIA container documented under runtime/.

fastapi>=0.110,<1
uvicorn[standard]>=0.27,<1
pymongo>=4.6,<5
mongomock>=4.1,<5
pytest>=8.0,<9
pydantic>=2.0,<3
